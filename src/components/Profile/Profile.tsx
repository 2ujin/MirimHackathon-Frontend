import styled from "styled-components";

const ProfileWrapper = styled.div`
  flex: 1;
  background-color: #a0fd83;
`;

function Profile() {
  return <ProfileWrapper>프로필 영역</ProfileWrapper>;
}

export default Profile;
