import styled from "styled-components";

const RepoWrapper = styled.div`
  flex: 4;
  background-color: #ffc964;
`;

function Repo() {
  return <RepoWrapper>저장소 영역</RepoWrapper>;
}

export default Repo;
